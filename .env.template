# This is a template for your environment configuration.

# --- Docker Configuration ---

# Your user ID for Docker containers
DOCKER_UID=`id -u`

# Your group ID for Docker containers
# This is needed if you want to have files created by the container
DOCKER_GID=`id -g`

# --- Database ---

# PostgreSQL database password (alphanumeric + special chars)
APP_DB_PASSWORD=<secret:32:alnum+special>

# Database port
APP_DB_PORT=5432

# Database connection URL (uses interpolation)
DATABASE_URL=postgres://user:${APP_DB_PASSWORD}@localhost:${APP_DB_PORT}/app

# --- Application ---

# Project name (will be slugified: "My App" -> "my-app")
PROJECT_SLUG=<slugify,required>

# Public URL for the application
PUBLIC_URL=<required,url>

# Contact email for the application
ADMIN_EMAIL=<required,email>

# Deployment environment
NODE_ENV=<development|staging|*production>

# Enable debug logging
DEBUG=<boolean>

# --- Security ---

# Session secret (hex for compatibility)
SESSION_SECRET=<secret:64:hex>

# API token (uppercase hex)
API_TOKEN=<secret:32:HEX>

# --- Optional: Stripe ---

# Enable Stripe billing
STRIPE_ENABLED=<boolean>

# Stripe API key (get from dashboard.stripe.com)
STRIPE_API_KEY=<if:STRIPE_ENABLED,required,regex:/^sk_[a-zA-Z0-9_]+$/:Enter a valid Stripe secret key>

# Stripe webhook secret
STRIPE_WEBHOOK_SECRET=<if:STRIPE_ENABLED>
