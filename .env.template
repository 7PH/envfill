# --- Docker Configuration ---

# Your user ID for Docker containers
DOCKER_UID=`id -u`

# Your group ID for Docker containers
# This is needed if you want to have files created by the container
DOCKER_GID=`id -g`

# --- Database ---

# PostgreSQL database password
APP_DB_PASSWORD=<secret:32>

# Database port
APP_DB_PORT=5432

# --- Application ---

# Public URL for the application
PUBLIC_URL=<required,url>

# Contact email for the application
ADMIN_EMAIL=<required,email>

# Deployment environment
NODE_ENV=<development|staging|*production>

# Enable debug logging
DEBUG=<boolean>

# --- Optional: Stripe ---

# Enable Stripe billing
STRIPE_ENABLED=<boolean>

# Stripe API key (get from dashboard.stripe.com)
STRIPE_API_KEY=<if:STRIPE_ENABLED,required,url>

# Stripe webhook secret
STRIPE_WEBHOOK_SECRET=<if:STRIPE_ENABLED>
